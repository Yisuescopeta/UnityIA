# Integration scenarios

The sandbox integration suite verifies:

- command console to dispatcher;
- HTTP to main-thread queue to dispatcher;
- CLI to HTTP to dispatcher;
- token rejection, request limits, timeout, idempotent retries, and domain
  reload session invalidation.

