# Cross-boundary tests

Contract fixtures in this directory are consumed by the .NET tests and mirrored
by package EditMode tests. Unity integration tests require the sandbox described
in `docs/development/sandbox.md`.

