# UnityIA Authoring package

Editor-only implementation of the UnityIA v0.1 controlled authoring protocol.
Install this folder as a local Unity package. The package does not add runtime
code to Player builds.

Use `Window > UnityIA > Command Console` to exercise the dispatcher before
using the HTTP transport.

